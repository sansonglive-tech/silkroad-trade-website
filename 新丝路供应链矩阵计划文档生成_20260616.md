# 新丝路·供应链矩阵计划 — 文档/PDF（v3 表格完整版）

**时间**: 2026-06-16 11:08
**输出文件**:
- `新丝路供应链矩阵计划.docx`（52.4 KB）
- `新丝路供应链矩阵计划.pdf`（335 KB）

## 本次改动

修复了"表格边框断线"问题，所有表格现在四条边框完整闭合：

1. **表级边框**：外框粗线（蓝色 #2E75B6，sz=6），内框细线（灰色 #999999，sz=4），表格整体统一
2. **每个单元格独立边框**：每个 cell 的 `<w:tcBorders>` 强制设为四个方向 solid（灰色 #666666，sz=4），确保不断线
3. **表头底边加粗**：表头每格底边用 8pt 蓝色线，与数据行明显区分
4. **数据行隔行底色**：奇数列白色、偶数列极浅蓝 #F5F8FC，阅读更舒适
5. **移除 `tblStyle` 冲突**：用 `tblBorders` + `tblLook` 完全接管边框渲染，不受默认样式干扰
6. **统一页眉页脚**：页眉右对齐"新丝路·供应链矩阵计划"，页脚居中页码
